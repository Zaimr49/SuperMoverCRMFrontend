const config = {
  API_BASE_URL: import.meta.env.VITE_BASE_URL,
  APP_ENV: import.meta.env.REACT_APP_ENV || "development",
  // API_BASE_URL: import.meta.env.VITE_API_BASE_URL || "https://default-api.com",

};

export default config;
